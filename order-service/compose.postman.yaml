services:
  order-service:
    environment:
      GATEWAY_ALLOWED_PEERS: 0.0.0.0/0,::/0
    ports:
      - "127.0.0.1:${SERVER_PORT:-8082}:8082"
    networks:
      - services
      - operations
      - postman

networks:
  postman:
    driver: bridge
